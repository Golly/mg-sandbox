(function() {
    'use strict';

    /*angular
        .module('app.layout')
        .config(layoutRoute);

    // appRun.$inject = ['$stateProvider']

    /* @ngInject */
    /*function layoutRoute($stateProvider) {
        var root = {
            name: 'root',
            url: '/',
            //abstract: true
            template: '<h1>Content container</h1>'
        };

        var rootDefault = {
            name: 'root.default',
            url: '',
            templateUrl: 'app/layout/default.layout.html'
        };

        $stateProvider
            .state(root);
            //.state(rootDefault);
    }*/
})();